extends layout

block content
  p 
    a(href="/new")= "New Message" 
  h1= title
  //- p Welcome to #{title}
  each message in messages
    ul 
      li= message.user
      li= message.text
      li= message.added